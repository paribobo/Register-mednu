rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
    
    match /registrations/{registrationId} {
      // Allow public access to register and view registration count
      allow create: if true;
      allow read: if true;
    }
  }
}
